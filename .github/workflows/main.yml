name: Build and Push Docker image

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: mohamedf332/mohamed-tareq_2023040218:latest
          file: ./Dockerfile
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: mohamedf332/mohamed-tareq_2023040218:latest
          file: ./Dockerfile

      - name: Confirm Success
        run: echo "Docker image built and pushed successfully"

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: mohamedf332/mohamed-tareq_2023040218:latest
          file: ./Dockerfile

      - name: Confirm Success
        run: echo "Docker image built and pushed successfully"

            - name: Build and push
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: mohamedf332/mohamed-tareq_2023040218:latest
          file: ./Dockerfile

      - name: Confirm Success
        run: echo " tttttttttttttttttt Docker image built and pushed successfully"

